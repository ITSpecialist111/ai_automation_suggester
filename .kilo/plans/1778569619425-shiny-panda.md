# Plan: Review Scripts Feature Completeness vs Automations

## Analysis Summary

After a full codebase review comparing the scripts reading feature against the automations reading feature, here is the complete comparison:

### ✅ Correctly Implemented (Scripts = Automations parity)

1. **Coordinator instance variables** (`coordinator.py:84-85`):
   - `self.script_read_file` ↔ `self.automation_read_file` ✅
   - `self.script_limit` ↔ `self.automation_limit` ✅

2. **`async_generate_suggestions()` parameters** (`coordinator.py:189-190`):
   - `script_read_yaml` ↔ `automation_read_yaml` ✅
   - `script_limit` ↔ `automation_limit` ✅

3. **Save/Restore in context manager** (`coordinator.py:205-206, 237-238`):
   - `script_read_file` and `script_limit` are saved and restored ✅

4. **`_build_prompt()` reads scripts** (`coordinator.py:430-433`):
   - `_read_scripts_default()` called ✅
   - `_read_scripts_file_method()` called conditionally ✅

5. **`_read_scripts_default()`** (`coordinator.py:495-510`):
   - Parallel to `_read_automations_default()` — reads `script` entities from HA state ✅

6. **`_read_scripts_file_method()`** (`coordinator.py:512-534`):
   - Parallel to `_read_automations_file_method()` — reads `scripts.yaml` file ✅
   - Correctly parses as `dict` (scripts.yaml is a dict, automations.yaml is a list) ✅

7. **Prompt text includes scripts** (`coordinator.py:446-449`):
   - "Scripts Overview" and "Scripts YAML Code" sections present ✅

8. **Service schema** (`__init__.py:66-67`):
   - `script_read_yaml` and `script_limit` in `GENERATE_SUGGESTIONS_SCHEMA` ✅

9. **Service call forwarding** (`__init__.py:111-112`):
   - `script_read_yaml` and `script_limit` passed to coordinator ✅

### ⚠️ Missing / Asymmetric (Scripts lack fields that Automations have)

1. **No `script_ids_used` field in `_normalise_suggestion()`** (`suggestions.py:192-210`):
   - The normalized suggestion dict includes `automation_ids_used` but does NOT include `script_ids_used`
   - This means if the AI suggests a new script, there's no way to track which existing scripts were referenced

2. **No `script_ids_used` extraction in `_try_loose_structured_items()`** (`suggestions.py:139`):
   - Only extracts `automation_ids_used`, not `script_ids_used`

3. **`STRUCTURED_OUTPUT_INSTRUCTIONS`** (`suggestions.py:20-37`):
   - The schema sent to the AI only lists `automation_ids_used`, no `script_ids_used`

4. **Frontend card** (`ai-automation-suggester-card.js`):
   - Does not display `script_ids_used` (but also doesn't display `automation_ids_used`, so this may be intentional)

### Recommendation

Add `script_ids_used` parity to match the automation side:
1. Add `"script_ids_used"` to `_normalise_suggestion()` return dict in `suggestions.py:205`
2. Add `"script_ids_used"` extraction in `_try_loose_structured_items()` in `suggestions.py:142`
3. Update `STRUCTURED_OUTPUT_INSTRUCTIONS` in `suggestions.py:29` to include `"script_ids_used": []`

This ensures scripts are treated as first-class citizens alongside automations in the suggestion tracking.